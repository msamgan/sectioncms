export const {underscoreCase} = {
    list: route('service.{underscoreCasePlural}'),
    show: (id) => route('service.{underscoreCase}.show', id),
    destroy: (id) => route('service.{underscoreCase}.destroy', id),
}
